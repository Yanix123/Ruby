# Supabase Postgres — Transaction Pooler connection string (port 6543), used at runtime
DATABASE_URL=
# Supabase Postgres — session-mode pooler (port 5432), used for migrations + seed
DIRECT_URL=
# Better Auth secret — at least 32 characters (generate: openssl rand -base64 32)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
